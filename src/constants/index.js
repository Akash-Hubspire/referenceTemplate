import images from './images';
import deviceTypes from './deviceTypes';
import deviceDimensions from './dimensions';
import navigation from './navigation';

export default {
  images,
  deviceTypes,
  deviceDimensions,
  navigation,
};
